---
layout: layouts/base.njk
title: "Análises de Jogos - VAR da Sorte"
---

<section class="page-header">
  <div class="container">
    <h1>📊 Análises de Jogos</h1>
    <p>Análises detalhadas dos jogos mais valiosos do dia, com estatísticas e insights estratégicos.</p>
  </div>
</section>

<section class="analyses-container container">
  {% if jogosDoDia and jogosDoDia | length %}
    {% set jogosValiosos = jogosDoDia | sort(attribute='oddsTotal') | reverse | slice(0,5) %}

    <div class="analyses-grid-wrapper">
      <div class="analyses-grid-main">
        {% for jogo in jogosValiosos %}
          <div class="analysis-card {% if loop.index <= 2 %}top-game{% endif %}">
            {% if loop.index == 1 %}
              <span class="badge">🔥 Top Game</span>
            {% elif loop.index == 2 %}
              <span class="badge">⭐ Destaque</span>
            {% endif %}

            <div class="game-top">
              <span class="club-name">{{ jogo.home }}</span>
              <span class="vs">vs</span>
              <span class="club-name">{{ jogo.away }}</span>
            </div>

            <p class="competition">{{ jogo.competition }}</p>
            <p class="game-date">Hora: {{ jogo.utcDate | date("HH:mm") }}</p>

            <div class="stats">
              <p><strong>Últimos 5 jogos:</strong> {{ jogo.home }} [W/D/L] - {{ jogo.away }} [W/D/L]</p>
              <p><strong>Odds:</strong> 1: {{ jogo.odds.home }} | X: {{ jogo.odds.draw }} | 2: {{ jogo.odds.away }}</p>
              <p><strong>Odd combinada:</strong> {{ jogo.oddsTotal }}</p>
            </div>

            <a href="/analises/{{ jogo.home | slugify }}-vs-{{ jogo.away | slugify }}" class="btn btn-primary btn-details">Ver Análise Completa</a>
          </div>
        {% endfor %}
      </div>

      <aside class="analyses-sidebar">
        <h3>Ligas do Dia</h3>
        <ul>
          {% set ligas = jogosDoDia | map(attribute='competition') | unique %}
          {% for liga in ligas %}
            <li>{{ liga }} ({{ jogosDoDia | selectattr('competition','equalto',liga) | list | length }} jogos)</li>
          {% endfor %}
        </ul>
        <h3>Resumo Estatístico</h3>
        <p>Os jogos mais valiosos destacam-se por odds combinadas e popularidade. Top games têm maior atenção para apostas estratégicas.</p>
      </aside>
    </div>

  {% else %}
    <p>⚠️ Em breve vais encontrar análises detalhadas dos jogos mais valiosos do dia. Mantém-te atento!</p>
  {% endif %}
</section>
