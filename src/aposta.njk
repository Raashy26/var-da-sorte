---
layout: layouts/base.njk
title: "Prognósticos Diários - VAR da Sorte"
---

<!-- TODO: Reativar sistema de filtros quando a secção de apostas for dinâmica -->


<section class="page-header reveal">
  <div class="hero-content">
    <h1>Prognósticos Diários</h1>
    <p>As melhores apostas, atualizadas todos os dias com base nas tuas preferências!</p>
  </div>
</section>

{# 🔒 Filtros temporariamente desativados — reativar quando o sistema de odds e favoritos estiver funcional #}
{#
<div class="filter-buttons">
  <button class="filter-btn active" data-filter="all">Todas</button>
  <button class="filter-btn" data-filter="favorites">⭐ Favoritas</button>
  <button class="filter-btn" data-filter="high-odds">🔥 Odds Altas</button>
</div>
#}


<section class="daily-bets reveal">
  <div class="container">
    {% set apostaHoje = collections.apostas | first %}

    {% if apostaHoje %}
      <div class="bet-highlight">
        <div class="bet-info">
          <h2>🎯 Prognósticos do Dia</h2>
          <p class="date">{{ apostaHoje.date | formatDate }}</p>
          <p class="desc">Descobre as apostas mais recomendadas de hoje, baseadas em análise e estatísticas exclusivas do VAR da Sorte.</p>
          <a href="{{ apostaHoje.url }}" class="btn btn-primary">Ver Apostas de Hoje</a>
        </div>
      </div>
    {% else %}
      <p class="no-bets">⚠️ Em breve vais encontrar aqui os prognósticos do dia. Mantém-te atento!</p>
    {% endif %}
  </div>
</section>



<section class="partners reveal">
  <div class="container">
    <h2>Principais casas de apostas em Portugal 🤝</h2>
    <div class="partners-grid">
      <div class="partner-card">
        <img src="/images/betano-logo.png" alt="Betano">
        <p>Aguarde novidades!</p>
        <a href="#" class="btn btn-secondary">Disponível em breve</a>
      </div>
      <div class="partner-card">
        <img src="/images/solverde-logo.jpg" alt="Solverde">
        <p>Aguarde novidades!</p>
        <a href="#" class="btn btn-secondary">Disponível em breve</a>
      </div>
      <div class="partner-card">
        <img src="/images/betclic-logo.png" alt="Betclic">
        <p>Aguarde novidades!</p>
        <a href="#" class="btn btn-secondary">Disponível em breve</a>
      </div>
    </div>
  </div>
</section>


<section class="historical-bets container reveal">
  <h2 class="section-title">📖 Histórico - Últimos 30 dias</h2>
  <div class="history-grid">
    {% for post in collections.apostas %}
      {% set diffDays = ((collections.apostas[0].date - post.date) / (1000*60*60*24)) | round %}
      {% if diffDays > 0 and diffDays < 30 %}
        <a href="{{ post.url }}" class="history-card">
          {{ post.date | date("dd/MM") }}
        </a>
      {% endif %}
    {% endfor %}
  </div>
</section>


<section class="cta container reveal">
  <div class="cta-wrapper" style="text-align:center; ">
    <h2>⚡ Participa nos Desafios Diários!</h2>
    <p>Transforma pequenas apostas em grandes lucros com os nossos desafios exclusivos. Acompanha a evolução e aprende com cada jogada.</p>
    <a href="/desafio/" class="btn btn-primary btn-cta">Ver Desafios</a>
  </div>
</section>

<script>

// 🔒 Sistema de filtros temporariamente desativado
/*

/* --- Filtro de apostas melhorado --- */
document.querySelectorAll(".filter-btn").forEach(btn => {
  btn.addEventListener("click", () => {
    const filter = btn.dataset.filter;

    // Atualiza botão ativo
    document.querySelectorAll(".filter-btn").forEach(b => b.classList.remove("active"));
    btn.classList.add("active");

    // Filtra cards
    document.querySelectorAll(".bet-card").forEach(card => {
      const odds = parseFloat(card.dataset.odds) || 0;
      const fav = card.dataset.favorite === "true";
      let show = false;

      if (filter === "all") show = true;
      if (filter === "favorites" && fav) show = true;
      if (filter === "high-odds" && odds >= 2.0) show = true;

      // animação suave ao mostrar/ocultar
      card.style.opacity = show ? "1" : "0";
      card.style.pointerEvents = show ? "auto" : "none";
      card.style.transform = show ? "translateY(0)" : "translateY(10px)";
      setTimeout(() => { card.style.display = show ? "block" : "none"; }, show ? 0 : 200);
    });
  });
});

*/

</script>


{# --- JSON-LD para Aposta --- #}
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "CollectionPage",
  "name": "{{ title or 'VAR da Sorte' }}",
  "url": "{{ site.url }}{{ page.url }}"
}
</script>

