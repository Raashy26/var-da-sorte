---
layout: layouts/base.njk
title: "Desafios de Apostas - VAR da Sorte"
---

<section class="page-header">
  <div class="container" style="text-align:center; margin-top:40px;">
    <h1>🔥 Desafios de Apostas</h1>
    <p>Três desafios especiais que podes acompanhar diariamente para testar estratégias e maximizar lucros.</p>
  </div>
</section>

<section class="filters container">
  <div class="filter-buttons">
    <button class="btn btn-secondary filter-btn" data-filter="all">Todos</button>
    <button class="btn btn-secondary filter-btn" data-filter="comeback">Desafio do Comeback ⚡</button>
    <button class="btn btn-secondary filter-btn" data-filter="draw">Desafio dos Empates ⚡</button>
    <button class="btn btn-secondary filter-btn" data-filter="over25">Ambas Marcam / +2.5 ⚡</button>
  </div>
</section>

<section class="daily-challenges container">
  {% set desafios = [
    {
      "type": "comeback",
      "title": "Desafio do Comeback",
      "description": "Apostar em viradas: seleciona jogos para tentar ganhar depois de estar a perder.",
      "odds": "Variável",
      "link": (collections.desafios_comeback[0].url if collections.desafios_comeback | length > 0 else "/desafios/comeback")
    },
    {
      "type": "draw",
      "title": "Desafio dos Empates",
      "description": "Escolher jogos propensos a empate, individuais e múltiplas.",
      "odds": "Variável",
      "link": (collections.desafios_draw[0].url if collections.desafios_draw | length > 0 else "/desafios/draw")
    },
    {
      "type": "over25",
      "title": "Desafio Ambas Marcam / +2.5",
      "description": "Criar múltipla com odd total ≥4.",
      "odds": "≥4",
      "link": (collections.desafios_over25[0].url if collections.desafios_over25 | length > 0 else "/desafios/over25")
    }
  ] %}

  <div class="challenges-grid">
    {% for desafio in desafios %}
      <div class="challenge-card" data-type="{{ desafio.type }}">
        <h3>{{ desafio.title }}</h3>
        <p>{{ desafio.description }}</p>
        <p><strong>Odd mínima:</strong> {{ desafio.odds }}</p>
        <a href="{{ desafio.link }}" class="btn btn-primary btn-details">Ver Detalhes</a>
      </div>
    {% endfor %}
  </div>
</section>

<section class="cta container">
  <div style="text-align:center; margin-top:40px;">
    <a href="/aposta/" class="btn btn-primary btn-lg">Ver Apostas Diárias 📅</a>
  </div>
</section>

<style>
.filters { margin: 40px 0 20px; text-align: center; }
.filter-buttons button { margin: 0 10px 10px 0; }

.challenges-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 25px;
}

.challenge-card {
  background-color: #eafaf1 !important;
  border-radius: 12px;
  padding: 20px;
  box-shadow: 0 4px 8px rgba(0,0,0,0.05);
  transition: transform 0.3s, box-shadow 0.3s;
}
.challenge-card:hover {
  transform: translateY(-5px);
  box-shadow: 0 8px 16px rgba(0,0,0,0.1);
}
.challenge-card h3 { color: #2d9c6a; margin-bottom: 10px; }
.challenge-card p { font-size: 0.9rem; margin: 5px 0; }

.btn-details {
  display: block;
  margin-top: 10px;
  text-align: center;
  background-color: #2d9c6a !important;
  color: #ffffff !important;
  border: none;
  border-radius: 8px;
  padding: 8px 14px;
  font-weight: bold;
  transition: background-color 0.3s;
}
.btn-details:hover {
  background-color: #218c5a !important;
}

@media (max-width: 768px) {
  .challenges-grid { grid-template-columns: 1fr; }
}


/* Garante que o footer fica sempre no fim */
body {
  display: flex;
  flex-direction: column;
  min-height: 100vh;
}
main {
  flex: 1;
}
</style>

<script>
const filterButtons = document.querySelectorAll(".filter-btn");
const challengeCards = document.querySelectorAll(".challenge-card");

filterButtons.forEach(btn => {
  btn.addEventListener("click", () => {
    const filter = btn.dataset.filter;
    challengeCards.forEach(card => {
      card.style.display = "block";
      if(filter !== "all" && card.dataset.type !== filter) {
        card.style.display = "none";
      }
    });
  });
});
</script>

{# --- JSON-LD para Desafio --- #}
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "CollectionPage",
  "name": "{{ title or 'VAR da Sorte' }}",
  "url": "{{ site.url }}{{ page.url }}"
}
</script>
